let Places_Name: Array<string> = [
  "SaifulMalook",
  "RohtasFort",
  "Hunza",
  "Swat",
  "Naran",
];

Places_Name.map((names: string) => {
  console.log(names);
});

Places_Name.sort().map((names: string) => {
  console.log(names);
});

Places_Name.map((names: string) => {
  console.log(names);
});

Places_Name.reverse().map((names: string) => {
  console.log(names);
});

Places_Name.map((names: string) => {
  console.log(names);
});

Places_Name.reverse().map((names: string) => {
  console.log(names);
});

Places_Name.reverse().map((names: string) => {
  console.log(names);
});

Places_Name.sort().map((names: string) => {
  console.log(names);
});

Places_Name.reverse()
  .sort()
  .map((names: string) => {
    console.log(names);
  });
